[pytest]
testpaths = tests
addopts = -p no:cacheprovider --strict-markers
markers =
    fs: file station tests
    ds: download station tests
    slow: tests that exercise long-running operations
timeout = 300
